{
  "name": "bharatsustain-ai",
  "version": "1.0.0",
  "private": true,
  "description": "India's adaptive sustainability intelligence and risk prediction operating system.",
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "npm --workspace apps/web run dev",
    "build": "npm --workspace apps/web run build",
    "lint": "npm --workspace apps/web run lint"
  }
}
